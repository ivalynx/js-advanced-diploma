export default class GameState {
  static from(object) {
    // TODO: create object
    const createdObject = object;
    // объект, который хранит текущее состояние игры
    // (может сам себя воссоздавать из другого объекта)
    return createdObject;
  }
}
